{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$FILE_PATH\" | grep -qE '^bug-issues/'; then if echo \"$FILE_PATH\" | grep -qE '^bug-issues/(bot7\\.md|README\\.md)$'; then exit 0; else echo '❌ Bot7 can only edit bug-issues/bot7.md' && exit 1; fi; fi; if echo \"$FILE_PATH\" | grep -qE '^(src/|\\.claude/skills/)'; then exit 0; else echo '❌ File creation only allowed in src/, .claude/skills/, and bug-issues/bot7.md' && exit 1; fi"
          }
        ]
      },
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$FILE_PATH\" | grep -qE '^bug-issues/'; then if echo \"$FILE_PATH\" | grep -qE '^bug-issues/(bot7\\.md|README\\.md)$'; then exit 0; else echo '❌ Bot7 can only edit bug-issues/bot7.md' && exit 1; fi; fi; if echo \"$FILE_PATH\" | grep -qE '^(src/|\\.claude/skills/)'; then exit 0; else echo '❌ File editing only allowed in src/, .claude/skills/, and bug-issues/bot7.md' && exit 1; fi"
          }
        ]
      }
    ]
  }
}
