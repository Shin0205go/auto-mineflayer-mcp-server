# Mineflayer MCP Server

**Claude AIエージェントがMinecraftを自律的にプレイするためのMCPサーバー**

```
    ┌────────────────────────┐
    │    🤖 Claude Bot       │  ← 自律的にサバイバル
    │                        │
    │   ⛏️ 採掘              │  ← 素材収集
    │   🔨 クラフト          │  ← 道具作成
    │   ⚔️ 戦闘              │  ← 敵と戦う
    │   🏠 建築              │  ← 構造物建築
    └────────────────────────┘
```

## 機能

### サバイバルモード
- **採掘・収集** - ブロック破壊、アイテム収集
- **クラフト** - 道具、ブロック作成
- **戦闘** - 敵モブと自動戦闘（武器装備、HP管理、逃走判断）
- **探索** - バイオーム検索、エンティティ検索

### マルチエージェント
- **複数ボット** - Claude, Claude2, ... を同時起動可能
- **エージェント掲示板** - 複数AI間のリアルタイム連携
- **ループ終了フック** - 次アクションを掲示板に自動記録

### 建築
- **ブロック設置** - 単一ブロック、構造物
- **プリセット** - 家、塔、村

## セットアップ

### 1. インストール

```bash
npm install
npm run build
```

### 2. 環境変数

```bash
cp .env.example .env
# MC_HOST, MC_PORT を設定
```

### 3. Minecraftサーバー準備

- Java Edition サーバーを起動
- **オペレーター権限が必要**（`/op Claude`）
- シングルプレイの「LANに公開」でもOK

## 起動方法

```bash
# Claudeエージェント起動
npm run start:claude

# 2体目のClaude（別名）
BOT_USERNAME=Claude2 MC_PORT=58896 npm run start:claude

# MCPサーバー（stdio）- Claude Desktop用
npm start

# WebSocket MCPサーバー
npm run start:mcp-ws

# 掲示板サーバー
npm run board
```

## 主要ツール

### 接続・移動
| ツール | 説明 |
|-------|------|
| `minecraft_connect` | サーバーに接続 |
| `minecraft_move_to` | 座標移動 |
| `minecraft_get_surroundings` | 周囲の状況確認 |

### サバイバル
| ツール | 説明 |
|-------|------|
| `minecraft_dig_block` | ブロック破壊 |
| `minecraft_craft` | クラフト |
| `minecraft_get_inventory` | インベントリ確認 |
| `minecraft_equip_item` | アイテム装備 |

### 戦闘
| ツール | 説明 |
|-------|------|
| `minecraft_fight` | 敵と戦う（自動装備・攻撃・逃走） |
| `minecraft_get_status` | HP/空腹確認 |
| `minecraft_eat` | 食事 |
| `minecraft_flee` | 逃走 |

### 探索
| ツール | 説明 |
|-------|------|
| `minecraft_get_biome` | バイオーム確認 |
| `minecraft_find_entities` | エンティティ検索 |
| `minecraft_explore_for_biome` | バイオーム探索 |

### エージェント連携
| ツール | 説明 |
|-------|------|
| `agent_board_read` | 掲示板を読む |
| `agent_board_write` | 掲示板に書く |
| `agent_board_wait` | 新着メッセージ待機 |

## 使用例

### 自律サバイバル

```
Claude: [minecraft_connect] → 接続
        [minecraft_get_surroundings] → 周囲確認
        [minecraft_dig_block] → 木を伐採
        [minecraft_craft item="stick"] → 棒をクラフト
        [minecraft_craft item="wooden_pickaxe"] → ピッケル作成
```

### マルチエージェント連携

```
Claude:  [agent_board_write] "木材集め担当します"
Claude2: [agent_board_read] → Claudeの報告を確認
         [agent_board_write] "採掘担当します"
```

## ディレクトリ構造

```
src/
├── index.ts              # MCPサーバー (stdio)
├── mcp-ws-server.ts      # MCPサーバー (WebSocket)
├── bot-manager.ts        # Mineflayerボット管理
├── realtime-board.ts     # 掲示板サーバー
├── tools/                # MCPツール
└── agent/                # Claudeエージェント
    ├── claude-agent.ts   # 自律エージェント
    └── claude-client.ts  # Claude SDK クライアント
```

## ライセンス

MIT
