# Mineflayer MCP Server

**AIエージェントの思考と行動をMinecraft上で可視化するMCPサーバー**

```
    ┌────────────────────────┐
    │    💭 パーティクル      │  ← 思考状態を表現
    │       ↓                │
    │    🤖 Bot              │  ← Mineflayer制御
    │    / | \               │
    │   🔥 ✨ 💚              │  ← 状態別エフェクト
    │                        │
    │   🏠 🗼 📍              │  ← 建築アクション
    └────────────────────────┘
```

## コンセプト

AIエージェント（Claude等）がタスクを実行する際の**内部状態を可視化**する。

- 今何を考えているか → パーティクル
- 何をしているか → 移動・建築

### 思考状態パーティクル

|状態          |パーティクル|意味      |
|------------|------|--------|
|`idle`      |灰色のダスト|待機中     |
|`processing`|炎     |処理中・計算中 |
|`searching` |エンチャント|情報収集中   |
|`executing` |緑の光   |アクション実行中|
|`error`     |赤い光   |エラー発生   |

## セットアップ

### 1. インストール

```bash
npm install
npm run build
```

### 2. Minecraftサーバー準備

- Java Edition サーバーを起動（ローカル or リモート）
- **オペレーター権限が必要**（`/setblock`、`/particle`コマンド用）
- シングルプレイの「LANに公開」でもOK

### 3. Claude Desktop / MCP クライアント設定

`claude_desktop_config.json` に追加:

```json
{
  "mcpServers": {
    "mineflayer": {
      "command": "node",
      "args": ["/path/to/mineflayer-mcp-server/dist/index.js"]
    }
  }
}
```

## ツール一覧

### 接続・基本

|ツール                     |説明      |
|------------------------|--------|
|`minecraft_connect`     |サーバーに接続 |
|`minecraft_disconnect`  |サーバーから切断|
|`minecraft_get_position`|現在位置を取得 |
|`minecraft_move_to`     |指定座標に移動 |
|`minecraft_chat`        |チャット送信  |

### 環境認識

|ツール                    |説明          |
|-----------------------|------------|
|`minecraft_look_around`|周囲のブロックをスキャン|

### 可視化

|ツール                           |説明            |
|------------------------------|--------------|
|`minecraft_visualize_thinking`|思考状態をパーティクルで表示|
|`minecraft_place_block`       |単一ブロック設置      |
|`minecraft_build_structure`   |プリセット構造物を建築   |

## 使用例

### 基本的な使い方

```
Human: Minecraftサーバー(localhost)に接続して、周りを見渡して

Claude: [minecraft_connect] → 接続成功
        [minecraft_visualize_thinking state="searching"] → 周囲を確認中...
        [minecraft_look_around] → 周辺ブロック情報取得
```

### 思考プロセスの可視化

```
Human: 家を建てて（思考過程も見せて）

Claude: [minecraft_visualize_thinking state="searching" message="建築場所を探索中..."]
        [minecraft_look_around]
        [minecraft_visualize_thinking state="processing" message="設計を検討中..."]
        [minecraft_visualize_thinking state="executing" message="建築開始！"]
        [minecraft_build_structure structure="house"]
```

## 今後の拡張案

- [ ] Pathfinding (mineflayer-pathfinder) 統合
- [ ] インベントリ管理ツール
- [ ] 複数ボット同時制御
- [ ] Prismarine-viewer でブラウザ表示

## ライセンス

MIT
